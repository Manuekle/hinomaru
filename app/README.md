# Hinomaru — app

SvelteKit web application. See the [root README](../README.md) for full setup instructions.

```bash
npm install
npm run dev       # localhost:5173
npm run build     # production build
npm run seed      # push content to Supabase (needs .env)
npm run check     # TypeScript check
```
